# On part d'une image officielle Node.js légère
FROM node:20-alpine

# On définit le dossier de travail dans le conteneur
WORKDIR /app

# On copie d'abord les dépendances (pour optimiser le cache Docker)
COPY package.json ./

# On installe les dépendances
RUN npm install

# On copie le reste du code
COPY . .

# On construit l'application Next.js
RUN npm run build

# Note : Next.js écoute sur le port 3000 par défaut
EXPOSE 3000

# La commande de démarrage
CMD ["npm", "start"]

